// 1 === "1"  Compares values and types. Output: false
// 1 == "1"   Converts number to string and compares only values. Output: true

if( 1 === "1") {
    showMessage("true");
}
else {
    showMessage("false");
}